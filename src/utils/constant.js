const TABLE_NAME = {
  TABLE_KEY: "key",
}

const ERROR_MESSAGE = {
  NOT_FOUND_KEY: "Requested key not found",
  KEY_FIELD_MISSING: "Key or Value missing to insert or update the data",
  ERROR_UPSERT_KEY: "Error While Updating the key"
}

export {
  TABLE_NAME,
  ERROR_MESSAGE,
}
